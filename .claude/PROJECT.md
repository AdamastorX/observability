# Pointer

Canonical context lives in the `adamastorx` repo:
`.claude/PROJECT.md` and `.claude/WORKFLOW.md`.

This repo (`observability`) = telemetry/SRE concern. Primary agent:
`observability-engineer` (see
`adamastorx/.claude/agents/observability-engineer.md`).
