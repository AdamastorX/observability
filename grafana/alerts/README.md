# grafana/alerts

Alert rules as code. Nothing here yet — M4, issue #21 ("Define SLOs and
alerting rules"), which depends on #20's golden-signal dashboards
(`../dashboards/README.md`) landing first: an SLO's error budget and an
alert's threshold both need real signal shapes to draw from, not a
guess made before the dashboards exist.
